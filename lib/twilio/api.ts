const API_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8000'

// Twilio-related API functions
// Note: Webhook endpoints are public and don't need auth, but we don't call them from frontend

// This file is for any future Twilio-related frontend API calls if needed
// Currently, all Twilio integration is handled via backend webhooks

export {} // Placeholder - no frontend Twilio API calls needed currently

